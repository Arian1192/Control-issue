# Coturn configuration — Control Issue TURN server
# https://github.com/coturn/coturn/wiki/turnserver

# Network
listening-port=3478
tls-listening-port=5349
min-port=${TURN_MIN_PORT:-56000}
max-port=${TURN_MAX_PORT:-56100}
fingerprint
no-cli

# Relay/public IPs — injected by start-coturn.sh from the runtime namespace.
# Do not set here; the script appends relay-ip=LOCAL plus external-ip=PUBLIC so
# Coturn binds locally on the host route but advertises only the public relay IP.

# TURN REST API authentication (RFC 8489 §9.2)
# Credentials are generated server-side with HMAC-SHA1
use-auth-secret
static-auth-secret=${TURN_SECRET}

# Realm — should match the domain serving the TURN server
realm=${TURN_REALM}

# Security: do not allow non-TURN traffic through the relay
no-multicast-peers
no-loopback-peers
denied-peer-ip=10.0.0.0-10.255.255.255
denied-peer-ip=172.16.0.0-172.31.255.255
denied-peer-ip=192.168.0.0-192.168.255.255

# Logging
log-file=stdout
simple-log
