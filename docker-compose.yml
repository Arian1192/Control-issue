services:
  app:
    build:
      context: .
      args:
        VITE_SUPABASE_URL: ${VITE_SUPABASE_URL}
        VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY}
        VITE_MESHCENTRAL_URL: ${VITE_MESHCENTRAL_URL:-}
    restart: unless-stopped
    expose:
      - '80'
    networks:
      - internal

  meshcentral:
    image: ghcr.io/ylianst/meshcentral:latest
    restart: unless-stopped
    volumes:
      - ./meshcentral:/opt/meshcentral/meshcentral-data
      - meshcentral-files-v4:/opt/meshcentral/meshcentral-files
    environment:
      - NODE_ENV=production
      - DYNAMIC_CONFIG=true
      - HOSTNAME=${MESHCENTRAL_HOSTNAME:-mesh.ariancoro.com}
    expose:
      - '443'
    networks:
      - internal

  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    environment:
      - TUNNEL_TOKEN=${CLOUDFLARE_TUNNEL_TOKEN}
    depends_on:
      - app
    networks:
      - internal

networks:
  internal:
    driver: bridge

volumes:
  meshcentral-files-v4:
